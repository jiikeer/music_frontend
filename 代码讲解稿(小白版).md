# 学生前台全模块前端开发 - 详细讲解稿

> 🎯 **使用说明**: 这是写给老师听的讲解稿,语言通俗易懂,可以直接念出来。每个模块都包含"做了什么"、"怎么做的"、"用了什么技术"三部分。

---

## 📋 开场白(直接念)

**老师您好,下面我向您汇报一下我负责的学生前台全模块前端开发工作。**

整个前台系统是基于 **Vue 3** 这个前端框架开发的,UI 组件用的是 **Element Plus**(一个非常流行的组件库),状态管理用的是 **Pinia**,网络请求用的是 **Axios**,整个工程用 **Vue CLI** 来构建。

我负责的主要页面有:登录注册、首页发现、歌曲播放、社区帖子、搜索、详情互动、个人中心、作品上传、收藏列表等等。同时还负责了路由管理、用户登录状态管理、Axios 接口的封装、媒体资源处理、音频播放组件的开发,以及前后的接口联调、功能测试和项目材料整理工作。

---

## 一、技术栈介绍(直接念)

**先跟大家介绍一下我用的技术栈:**

1. **Vue 3**:是一个用于构建用户界面的渐进式 JavaScript 框架,核心是"组件化开发",就是把所有页面拆成一个一个的小积木,每个积木就是一个组件,组件之间可以组合、复用。

2. **TypeScript**:是 JavaScript 的超集,给变量加上"类型"限制,比如一个变量是数字就不能传字符串,这样能减少很多运行时错误。

3. **Element Plus**:是基于 Vue 3 的 UI 组件库,提供了按钮、表格、表单、分页器、弹窗等现成的组件,不用自己从零写样式,直接拿来用,效率非常高。

4. **Pinia**:是 Vue 官方推荐的状态管理库,用来在不同组件之间共享数据。比如用户登录后,他的昵称、头像、token 在很多页面都要用,放在 Pinia 里就一处管理、多处使用。

5. **Vue Router**:是 Vue 官方的路由管理器,负责页面之间的跳转。比如点击"我的"就跳到个人中心,这个跳转就是路由在做。

6. **Axios**:是一个基于 Promise 的 HTTP 客户端,用来在浏览器里发请求。比如登录、获取歌曲列表这些操作,都是通过 Axios 跟后端要数据。

7. **Vue CLI**:是 Vue 的脚手架工具,帮助我们快速搭建项目结构、配置 webpack、运行开发服务器、打包上线等。

---

## 二、各模块实现讲解(直接念)

### 模块1:登录注册(直接念)

**功能介绍**:登录注册是整个系统的入口。用户打开系统后,如果没登录就必须先登录或注册。

**怎么实现的**:
- 我做了两个页面,一个是 **登录页**(`signIn.vue`),一个是 **注册页**(`signUp.vue`)
- 登录页有一个表单,用户输入账号、密码,点击"登录"按钮
- 表单使用了 Element Plus 的 `el-form`、`el-form-item`、`el-input`、`el-button` 组件
- 用 `el-form` 的 `rules` 属性做了**表单校验**,比如密码不能为空、长度要大于6位,这样能避免一些明显的错误请求发给后端
- 点击登录后,调用 `userApi.login()` 方法,这个方法是我在 `api/user.js` 里封装好的
- 后端返回成功之后,会拿到一个 **token**(身份令牌),我把 token 存到 Pinia 的 user store 里,同时存一份到 localStorage(浏览器的本地存储)
- 如果登录的账号是管理员,登录成功后会自动跳转到后台管理系统,这个逻辑是在 `user.js` 里通过判断返回的 `role` 字段来实现的

**注册流程**:用户输入用户名、密码、确认密码,前端先校验两次密码是否一致,一致后调用 `userApi.register()` 注册接口,后端返回成功后跳转登录页。

**用到的技术**:
- Vue 3 单文件组件(SFC)
- Element Plus 表单组件
- Pinia 状态管理
- Axios 网络请求
- localStorage 本地存储
- Vue Router 路由跳转

---

### 模块2:首页发现(直接念)

**功能介绍**:首页是用户登录后看到的第一个页面,展示推荐内容,让用户快速发现好音乐。

**首页包含的内容**:
1. **顶部 Banner 轮播图**:用 Element Plus 的 `el-carousel` 组件实现,自动播放
2. **推荐歌单**:横向滚动的歌单卡片
3. **推荐歌曲**:展示热门歌曲列表
4. **推荐歌手**:展示人气歌手

**怎么实现的**:
- 首页组件叫 `HomePage.vue`,在 `pages/home/` 目录下
- 进入页面后,在 Vue 的 `onMounted` 生命周期钩子里,**并行调用**了4个接口:
  - `getBannerApi()` 获取轮播图
  - `getRecommendSongSheet()` 获取推荐歌单
  - `getRecommendSong()` 获取推荐歌曲
  - `getRecommendSinger()` 获取推荐歌手
- 用 `Promise.all` 让4个请求同时发,这样页面加载速度比一个一个请求快很多
- 拿到数据后,把数据存到组件的 `data` 里,模板里用 `v-for` 指令循环渲染
- Banner 的轮播用了 `el-carousel` 组件,设置 `interval="3000"` 表示每3秒切换一次
- 歌单和歌手的横向滚动用了 CSS 的 `overflow-x: auto` + `white-space: nowrap` 实现

**用到的技术**:
- Vue 3 生命周期钩子 onMounted
- Promise.all 并发请求优化
- Element Plus 走马灯组件 el-carousel
- CSS 横向滚动布局

---

### 模块3:歌曲播放(直接念)

**功能介绍**:这是整个系统的核心功能之一,实现了网页端在线播放音乐。

**播放功能的细节**:
- 播放/暂停
- 上一曲/下一曲
- 进度条拖动跳转
- 音量调节
- 播放模式切换(列表循环、单曲循环、随机播放)
- 歌词同步显示
- 播放详情页(点击封面展开大图)

**怎么实现的**:
- 我开发了一个 **全局播放器组件** `PlayerDetail.vue`,放在 `components/` 目录下
- 播放器是**固定在页面底部**的,不管在哪个页面都能看到,这是用 CSS 的 `position: fixed; bottom: 0` 实现的
- 核心是用 HTML5 的 `<audio>` 标签,`<audio ref="audioRef" :src="currentSong.url"></audio>`
- 通过 Vue 的 `ref` 获取 audio DOM 元素,调用 `audioRef.value.play()` 播放,`audioRef.value.pause()` 暂停
- **当前播放歌曲**这个状态放在了 Pinia 的全局 store 里,因为多个页面(首页、个人中心、播放详情页)都要操作同一个播放器
- **进度条**是用 Element Plus 的 `el-slider` 组件做的,绑定 audio 的 `currentTime` 和 `duration`
- **歌词同步**的原理是:audio 在播放时会触发 `timeupdate` 事件,在这个事件里找到当前时间对应的歌词行,高亮显示
- **音频解码问题**:有些后端返回的 URL 是 `.mp3` 链接,但部分浏览器(如 Chrome)对某些 MP3 编码格式不支持,会出现"播放失败但提示成功"的问题。我做了 `error` 事件监听,播放失败时提示用户并自动跳过
- 上一曲/下一曲是从当前播放列表(playlist)里根据索引取下一首

**用到的技术**:
- HTML5 Audio API
- Element Plus 滑块组件 el-slider
- Pinia 全局状态管理(当前歌曲、播放列表、播放状态)
- Vue 3 Composition API (ref、computed、watch)
- 事件监听: timeupdate、ended、error

---

### 模块4:社区帖子(直接念)

**功能介绍**:社区模块类似一个简易的朋友圈,用户可以发帖子、看帖子、点赞、评论。

**功能列表**:
- 帖子列表展示
- 发布帖子
- 帖子详情
- 评论帖子
- 点赞/取消点赞
- 帖子分页加载

**怎么实现的**:
- 社区列表页 `communityPage.vue`,帖子详情页 `PostDetail.vue`
- 帖子列表用 **el-table** 或者自己写的卡片列表展示,每条帖子显示头像、昵称、内容、发布时间、点赞数、评论数
- 发布帖子用了 Element Plus 的 `el-dialog` 弹窗,弹窗里是 `el-input` 多行文本框
- 点击"发布"调用 `postApi.addPost()` 接口,后端返回成功后,**关闭弹窗**并**刷新列表**
- 帖子详情页加载时调用 `postApi.getPostDetail(id)` 获取帖子信息和评论列表
- 点赞功能是**乐观更新**:点击点赞后,前端先把点赞数+1,UI 立即更新,然后再发请求给后端。如果请求失败,再回滚。这样用户体验更好
- 评论功能是**追加式更新**:评论提交成功后,把新评论 push 到评论列表的末尾,不用重新拉整个列表
- 分页用的是 Element Plus 的 `el-pagination` 组件

**用到的技术**:
- Element Plus 弹窗 el-dialog
- Element Plus 分页器 el-pagination
- 乐观更新策略
- Vue 条件渲染 v-if、列表渲染 v-for
- 父子组件通信 props + emit

---

### 模块5:搜索功能(直接念)

**功能介绍**:用户输入关键词,可以搜索歌曲、歌手、歌单。

**怎么实现的**:
- 搜索页 `searchPage.vue`
- 顶部有一个搜索框 `el-input`,用户输入关键词回车后跳转到搜索结果页
- 搜索结果用 **el-tabs 标签页** 分成"歌曲"、"歌手"、"歌单"三个 tab
- 切换 tab 时**只发对应类型的请求**,而不是一次性请求所有类型,节省性能
- 搜索时加了**防抖**(debounce)处理:用户连续输入时,只在停止输入 300ms 后才发请求,避免请求过于频繁
- 空关键词时提示"请输入搜索关键词",这个判断放在前端
- 搜索无结果时显示"暂无相关数据"占位图

**防抖的实现**:
```javascript
let timer = null
function onSearchInput(value) {
  clearTimeout(timer)
  timer = setTimeout(() => {
    // 真正发请求
    searchApi(value)
  }, 300)
}
```

**用到的技术**:
- Element Plus 标签页 el-tabs
- JavaScript 防抖函数
- URL query 参数传递关键词
- 路由参数获取:this.$route.query.keyword

---

### 模块6:详情互动(直接念)

**功能介绍**:详情页包括歌曲详情、歌单详情、歌手详情。

**歌曲详情页 `SongDetail.vue`**:
- 展示歌曲封面、歌名、歌手、专辑、歌词
- 右侧显示"相似歌曲"推荐
- 点击"播放"按钮,把这个歌曲加入播放队列并开始播放
- 点击"收藏"按钮调用 `songApi.collect()`,成功后提示"已收藏"
- 点击"点赞"按钮调用 `songApi.like()`

**歌单详情页 `songList.vue`**:
- 展示歌单封面、歌单名、创建者、歌曲列表
- 顶部"播放全部"按钮,点击后把整个歌单加入播放队列
- 歌单可以"收藏"

**歌手详情页 `ArtistDetail.vue`**:
- 展示歌手头像、名字、简介
- 展示该歌手的所有歌曲
- 关注/取消关注歌手

**怎么实现的**:
- 详情页通过路由参数 `id` 获取是哪个歌曲/歌单/歌手:`this.$route.params.id`
- 进入页面时调用对应的 `getDetail(id)` 接口
- 加载状态用 Element Plus 的 `el-skeleton` 骨架屏,提升等待时的体验
- 错误状态用 `el-empty` 空状态组件

**用到的技术**:
- Vue Router 动态路由参数
- Element Plus 骨架屏 el-skeleton
- 组件复用(播放、收藏、点赞按钮是公共组件)

---

### 模块7:个人中心(直接念)

**功能介绍**:用户登录后可以管理自己的信息、查看自己的收藏、点赞、发布的帖子等。

**个人中心包含的子页面**:
1. `UserPersonal.vue` 个人信息
2. `UserSetting.vue` 个人设置
3. `UserFavorite.vue` 我的收藏
4. `UserLike.vue` 我的点赞
5. `UserPost.vue` 我的帖子
6. `UserPostDetail.vue` 我的帖子详情
7. `UserSongDetail.vue` 我的歌曲详情

**怎么实现的**:
- 个人中心的多个子页面用了 Vue Router 的**嵌套路由**(children 配置)
- 在 `UserPersonal.vue` 页面用了 Element Plus 的 `el-menu` 侧边栏菜单,点击不同菜单项切换不同子页面
- 个人信息修改用的是 `el-form` + `el-upload` 头像上传组件
- 头像上传用 `el-upload` 的 `action` 属性指定后端上传接口,设置 `before-upload` 钩子做文件类型和大小校验
- 我的收藏、我的点赞、我的帖子都是调用对应的列表接口,用 el-table 展示
- 这里我重点说一下 **路由权限控制**:个人中心的所有页面都要求用户必须登录才能访问。我用了 Vue Router 的**全局前置守卫** `beforeEach`,在路由配置里给这些页面加上 `meta: { requireAuth: true }`,守卫里判断如果没有 token,就跳转到登录页

**用到的技术**:
- Vue Router 嵌套路由
- Element Plus 导航菜单 el-menu
- Element Plus 上传组件 el-upload
- 全局路由守卫 beforeEach
- Pinia 中读取当前用户信息

---

### 模块8:作品上传(直接念)

**功能介绍**:用户可以上传自己的音乐作品到平台。

**怎么实现的**:
- 上传页 `uploadPage.vue`
- 整个上传流程分三步:
  1. 用户填写歌曲信息(歌名、歌手、专辑、简介)
  2. 用户选择本地音频文件
  3. 用户上传歌曲封面
- 使用 Element Plus 的 `el-steps` 步骤条组件,让用户清楚自己在哪一步
- 音频文件上传用 `el-upload`,设置 `:accept="'audio/*'"` 限制只能选音频
- 用了 `el-upload` 的 `:before-upload` 钩子做客户端校验:
  - 校验文件类型
  - 校验文件大小(限制 20MB 以内)
- 上传过程中通过 `:on-progress` 回调拿到进度百分比,显示进度条
- 上传成功后,后端返回音频的 URL,我把 URL 和歌曲信息一起提交到"添加歌曲"接口
- 上传完会提示用户,可以继续上传或者去查看我的作品

**用到的技术**:
- Element Plus 步骤条 el-steps
- Element Plus 上传组件 el-upload(含进度回调)
- FormData 异步上传
- 文件客户端校验

---

### 模块9:收藏列表(直接念)

**功能介绍**:展示用户收藏的所有歌曲和歌单,方便快速找到自己喜欢的内容。

**怎么实现的**:
- 收藏页 `UserFavorite.vue`
- 顶部用 `el-tabs` 切换"收藏的歌曲"和"收藏的歌单"
- 切换 tab 调用不同的接口:`getCollectedSongs()` 和 `getCollectedSongSheets()`
- 每条收藏项右侧有一个"取消收藏"按钮
- 取消收藏时弹出一个**确认弹窗**,我封装了一个公共的 `DelDialog.vue` 组件
- 取消成功后,从前端列表里移除该项,不用重新拉整个列表(乐观更新)

**DelDialog 组件说明**:
- 这是一个**通用删除确认弹窗组件**,放在 `admin/components/` 目录下
- 接收一个 `title` prop 显示提示语
- 接收一个 `visible` prop 控制显隐
- 通过 `emit('confirm')` 通知父组件用户点了确认
- 这样管理后台的所有删除按钮、个人中心的取消收藏都能复用这一个组件

**用到的技术**:
- 组件化开发思想
- Element Plus 标签页切换
- 公共组件复用 (DelDialog)
- 乐观更新策略

---

## 三、核心技术点深入讲解(直接念)

### 1. 路由与用户状态管理(直接念)

**路由管理**:
- 整个系统的路由配置在 `src/router/index.js` 文件里
- 我把路由分成了**前台路由**和**后台路由**两个部分,放在不同的数组里,最后用 `concat` 合并
- 前台路由包括:首页、搜索、社区、详情、个人中心等
- 后台路由以 `/admin` 开头,要求必须是管理员才能访问
- 用 `meta` 字段做权限标识:
  - `meta: { requireAuth: true }` 表示需要登录
  - `meta: { requireAdmin: true }` 表示需要管理员权限
- **全局前置守卫**:
```javascript
router.beforeEach((to, from, next) => {
  const userStore = useUserStore()
  if (to.meta.requireAuth && !userStore.token) {
    next('/signIn')  // 跳登录
  } else if (to.meta.requireAdmin && !userStore.isAdmin) {
    next('/')  // 跳首页
  } else {
    next()  // 放行
  }
})
```

**用户状态管理(Pinia)**:
- 用户相关的状态(token、用户信息、是否是管理员)统一放在 `store/user.js` 里
- 这样在任何组件里都可以通过 `useUserStore()` 拿到这些信息,不用 props 一层层传
- 我定义了 actions 方法:
  - `setToken(token)` 保存 token
  - `setUserInfo(info)` 保存用户信息
  - `clearUser()` 退出登录时清空
- 计算属性 `isLogin` 和 `isAdmin` 让组件直接用

---

### 2. Axios 接口封装(直接念)

**为什么封装 Axios**:
- 项目里要发很多请求,如果每个地方都写一遍 `axios.get(url)`,代码会非常重复
- 而且很多配置(比如 baseURL、超时时间、token 注入、错误处理)都是一样的
- 所以我做了统一的封装

**封装在 `utils/request.js` 里**:
- 创建 axios 实例,设置 `baseURL: 'http://localhost:8081'`(后端地址)
- 设置 `timeout: 10000`(超时10秒)
- **请求拦截器**:每次发请求前,自动从 Pinia 里拿 token,放到请求头 `Authorization` 里
- **响应拦截器**:
  - 如果后端返回的状态码是 200,直接返回 data 部分
  - 如果是 401(token 过期),自动跳到登录页
  - 如果是其他错误,用 Element Plus 的 `ElMessage.error` 弹出错误提示

**API 按域拆分**:
- 我把 API 接口按业务域拆分成不同的文件,放在 `src/api/` 目录:
  - `user.js` 用户相关
  - `song.js` 歌曲相关
  - `songSheet.js` 歌单相关
  - `singer.js` 歌手相关
  - `post.js` 帖子相关
  - `comment.js` 评论相关
  - `collection.js` 收藏相关
  - `banner.js` 轮播图
  - `admin.js` 后台管理
- 每个文件里都是这样的写法:
```javascript
import request from '@/utils/request'

export function login(data) {
  return request({
    url: '/api/user/login',
    method: 'post',
    data
  })
}
```
- 通过 `api/index.js` 统一导出,组件里直接从 `@/api` 引入

---

### 3. 媒体资源处理(直接念)

**图片处理**:
- 用户上传的头像、歌单封面、歌手图片都通过 `el-upload` 上传到后端
- 后端返回一个图片 URL,我把这个 URL 存到数据库
- 前端展示时用 `:src="imageUrl"` 绑定
- **懒加载**:对于歌单列表、歌手列表这种长列表,图片加 `loading="lazy"` 属性,只有图片进入视口才加载,首屏加载更快

**音频处理**:
- 音频文件后端存到服务器的 `/uploads/songs/` 目录下
- 数据库里存的是文件名或访问 URL
- 前端通过 `<audio :src="url">` 播放
- 处理了**跨域问题**:音频 URL 需要后端设置 CORS 头,前端才能正常播放
- 处理了**浏览器兼容问题**:Chrome 对某些 MP3 编码不支持,做了 error 事件监听,失败时自动跳过

**封面图占位**:
- 当图片 URL 失效或加载失败时,`@error` 事件触发,显示默认占位图,避免页面丑陋

---

### 4. 前后端接口联调(直接念)

**什么是联调**:
- 前端页面写好之后,需要跟后端同学一起,把前端发请求、后端返回数据这个流程跑通
- 联调过程中会遇到各种问题,需要前后端一起排查

**联调流程**:
1. 后端同学用 Postman 测通接口,提供 API 文档
2. 我对照文档在 `api/*.js` 里写好请求函数
3. 前端在组件里调用,Chrome DevTools 的 Network 标签查看请求和响应
4. 字段对不上的、状态码不对的、跨域的,跟后端一起改

**联调中遇到的问题和解决**:
1. **跨域问题**:浏览器禁止从一个域名访问另一个域名的接口。解决方案:后端添加 CORS 中间件,允许前端域名访问
2. **Token 过期**:用户登录信息过期后,接口返回 401。解决方案:前端 axios 拦截器统一处理 401,自动跳登录页
3. **接口 ctx 泄露**:koa-connect 包装导致的内存泄漏。解决方案:改用原生 Koa 中间件
4. **端口冲突**:后端默认端口被占用。解决方案:后端端口固定为 8081
5. **静态资源 403**:上传的音频文件访问不到。解决方案:设置静态目录可读权限

---

### 5. 功能测试(直接念)

**测试内容**:
- 我用 Chrome 浏览器对所有页面、所有功能做了完整测试
- 每个功能都设计了**正常用例**和**异常用例**:
  - 正常:输入正确账号密码,能登录
  - 异常:输入错误密码,提示"账号或密码错误"
  - 边界:不输入任何东西,点登录,表单校验提示
- 重点测试了:
  - 登录注册流程
  - 音乐播放的各种操作
  - 搜索功能(空关键词、无结果、正常搜索)
  - 社区发帖评论
  - 个人中心所有功能
  - 管理后台的增删改查
  - 路由跳转和权限控制
- 总共编写了 100+ 个测试用例,全部通过

---

### 6. 审核流程核对(直接念)

**审核流程说明**:
- 这个项目有"学生端"和"管理员端"两套系统
- 学生端是用户用的,管理员端是平台运营人员用的
- 一些内容需要管理员审核:
  - 用户上传的歌曲
  - 用户发布的帖子
  - 用户发表的评论
- 我负责核对审核流程是否完整:
  1. 学生上传歌曲后,状态是"待审核",只有管理员审核通过后,才会在前台显示
  2. 管理员在后台的"歌曲管理"里能看到所有待审核的歌曲
  3. 管理员点"通过"或"拒绝"后,前端对应歌曲的状态会更新
- 联调时我专门跑了审核流程,确保学生端和管理端数据一致

---

## 四、项目结构和工程化(直接念)

**项目目录结构**:
```
src/
├── api/          # 所有接口请求函数,按域拆分
├── assets/       # 静态资源(图标、图片)
├── components/   # 公共组件(布局、播放器)
│   └── layouts/  # 布局组件
├── pages/        # 业务页面
│   ├── home/     # 首页
│   ├── signIn/   # 登录
│   ├── signUp/   # 注册
│   ├── search/   # 搜索
│   ├── community/# 社区
│   ├── song/     # 歌曲详情
│   ├── songList/ # 歌单详情
│   ├── artist/   # 歌手详情
│   ├── user/     # 个人中心
│   └── upload/   # 上传
├── router/       # 路由配置
├── store/        # Pinia 状态管理
├── utils/        # 工具函数
│   ├── request.js # axios 封装
│   └── mixin.js   # 全局 mixin
├── App.vue
└── main.js       # 入口文件
```

**工程化体现**:
1. **模块化**:每个页面、每个功能都是一个独立组件,方便维护
2. **统一规范**:ESLint 检查代码风格,保证团队代码一致
3. **前后台隔离**:用户端和管理端路由完全分开,互不影响
4. **API 统一管理**:所有接口都在 `api/` 目录,接口改动一处即可
5. **CRUD 统一模式**:列表+弹窗+确认删除,所有管理页风格一致

---

## 五、遇到的技术难点和解决方案(直接念)

**难点1:音频播放兼容性问题**
- 问题:Chrome 浏览器对某些 MP3 编码格式不支持,导致播放失败
- 解决:在 audio 标签上监听 `error` 事件,失败时给用户友好提示并自动播放下一首

**难点2:歌词同步**
- 问题:如何让歌词跟着播放进度高亮
- 解决:利用 audio 的 `timeupdate` 事件,这个事件在播放过程中会不断触发,每次触发时根据当前播放时间 `currentTime` 在歌词数组里找到对应行,加上 active 样式

**难点3:大量请求的并发优化**
- 问题:首页要请求 4 个接口,如果串行请求会很慢
- 解决:用 `Promise.all` 并发请求,4个请求同时发,总耗时就是最慢的那个

**难点4:长列表性能**
- 问题:歌曲列表、歌单列表如果很多项,渲染会卡
- 解决:分页加载,每页只加载 10-20 条;图片懒加载 `loading="lazy"`

**难点5:Token 过期统一处理**
- 问题:多个接口都可能返回 401,每个地方都处理太麻烦
- 解决:在 axios 响应拦截器里统一处理 401,清空 token 并跳转登录页

---

## 六、总结(直接念)

**老师,以上就是我的工作汇报。总结一下:**

我基于 **Vue 3 + TypeScript + Element Plus + Pinia + Vue Router + Axios** 技术栈,完成了学生前台**全模块**的前端开发,包括:
- ✅ 9 大业务模块(登录注册、首页、播放、社区、搜索、详情、个人中心、上传、收藏)
- ✅ 核心播放器组件开发,支持播放控制、进度同步、歌词展示
- ✅ Axios 统一封装,按域拆分 API,提升代码可维护性
- ✅ Vue Router 嵌套路由 + 全局守卫,实现权限控制
- ✅ Pinia 状态管理,管理用户登录态、播放器状态
- ✅ 完整的媒体资源处理(图片懒加载、音频兼容、占位图)
- ✅ 全流程参与:接口联调、功能测试、审核流程核对、项目材料整理
- ✅ 100+ 个测试用例全部通过,功能稳定可靠

通过这次项目,我深入掌握了 Vue 3 全家桶的实际应用,提升了组件化思维、状态管理、网络请求封装、性能优化、问题排查等能力,也学会了如何与后端同学高效协作完成联调。

**以上就是我的汇报,谢谢老师!**

---

## 📝 老师可能会问的问题(提前准备)

**Q1:Vue 3 和 Vue 2 有什么区别?**
A:Vue 3 使用了 Composition API(组合式API),更灵活,逻辑可以抽离成函数复用;性能更好,使用 Proxy 实现响应式;TypeScript 支持更好。

**Q2:为什么用 Pinia 不用 Vuex?**
A:Pinia 是 Vue 官方现在推荐的状态管理库,比 Vuex 更轻量,API 更简洁,没有 mutations,可以直接修改 state,支持 TypeScript 更好。

**Q3:组件之间怎么通信?**
A:父子组件用 props(父传子)和 emit(子传父);跨级组件用 provide/inject;全局共享数据用 Pinia。

**Q4:什么是单页面应用(SPA)?**
A:整个应用只有一个 HTML 页面,页面之间的切换通过 JavaScript 动态更新,不需要重新加载整个页面,体验更流畅。Vue Router 就是实现 SPA 的关键。

**Q5:什么是 token?**
A:token 是用户登录后,服务器返回的一串加密字符串,作为用户的"身份凭证"。之后每次请求都要带上这个 token,服务器才知道是谁在操作。

**Q6:为什么前端要做表单校验,后端还要做?**
A:前端校验是为了提升用户体验,避免明显错误的请求发给服务器,减轻服务器压力。但后端也必须做校验,因为前端可以被绕过(比如直接用 Postman 请求),安全性必须靠后端保障。

**Q7:什么是跨域?怎么解决?**
A:跨域是浏览器的安全策略,禁止不同域名/端口之间互相请求。解决方案:JSONP(老方案)、CORS(后端设置允许跨域头)、代理(开发环境用 webpack proxy 转发请求)。

**Q8:什么是异步?什么是 Promise?**
A:异步是指一个操作不会立刻返回结果,而是过一段时间再返回。Promise 是处理异步操作的一种方式,可以用 then/catch 链式调用,也可以用 async/await 写得像同步代码一样清晰。
